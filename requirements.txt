# Core ML (pin torch for pyannote compatibility)
torch==2.5.1
torchaudio==2.5.1

# ASR - WhisperX (includes faster-whisper, pyannote)
whisperx

# Translation
transformers>=4.35.0
accelerate>=0.25.0
sentencepiece>=0.1.99
safetensors>=0.4.0
protobuf>=3.20.0

# Audio Processing
numpy>=1.24.0
scipy>=1.11.0
soundfile>=0.12.0
python-dotenv

# Deployment (Modal Cloud)
modal

# Web Framework
fastapi>=0.104.0
uvicorn>=0.24.0
websockets>=12.0
aiofiles>=23.0.0

# Dev & Testing
pytest
ruff
